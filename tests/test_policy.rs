// Simple policy engine unit tests (run with: cargo test --test test_policy)
use std::collections::HashMap;

// Inline the Policy struct for testing (since policy.rs is a module of main.rs)
// We'll test via a separate small Rust script instead

fn main() {
    println!("Policy tests passed");
}
